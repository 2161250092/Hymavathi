#include<stdio.h>     
int main()
{
    int arr[50],i,j,ele,n;
    printf("enter the size of the array");
    scanf("%d",&n);
    printf("enter the elements into array");
    for(i=0;i<n;i++)
    scanf("%d",&arr[i]);
    arr[i]=ele; 
    while(i<=n)
    {
        for(j=1;j<n;j++)
        {
            scanf("%d",&ele);
        }
        i++;
    }
    if(arr[i]==ele)
    printf("DUPLICATE");
    else
    printf("UNIQUE");
    return 0;
}
